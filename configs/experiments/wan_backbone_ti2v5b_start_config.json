{
  "model_variant": "wan_backbone",
  "data_root": "sample_data/WAI_prepared/sequences",
  "output_dir": "outputs/cointeract_wan_backbone_ti2v5b",
  "seed": 42,
  "mixed_precision": "bf16",
  "batch_size": 1,
  "gradient_accumulation_steps": 1,
  "max_steps": 1000,
  "save_every": 500,
  "lr": 1e-05,
  "warmup_steps": 100,
  "lr_scheduler": "constant",
  "clip_length": 1,
  "coordinate_mode": "relative",
  "image_height": 256,
  "image_width": 256,
  "hidden_dim": 512,
  "wan_model_id": "Wan-AI/Wan2.2-TI2V-5B-Diffusers",
  "wan_dtype": "bf16",
  "wan_hidden_dim": 3072,
  "wan_prompt_max_sequence_length": 512,
  "wan_local_files_only": true,
  "freeze_wan": true,
  "detach_rgb_context": true,
  "num_human_gaussians": 850,
  "num_object_gaussians": 850,
  "lambda_state_fm": 1.0,
  "lambda_rgb_fm": 0.0,
  "lambda_hoi_token_router": 0.0,
  "notes": "The wan_backbone model ignores the local hidden_dim/depth/num_heads DiT settings and uses wan_hidden_dim plus the pretrained Wan transformer.blocks loaded from wan_model_id."
}
